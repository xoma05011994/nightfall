import { WORLD_HALF_SIZE } from "@nightfall/shared/constants";

// Placeholder entry point — confirms the server workspace can resolve
// @nightfall/shared. Actual RivetKit actors (matchmaker, match) land in M1.
console.log(`Nightfall server scaffold OK — shared WORLD_HALF_SIZE=${WORLD_HALF_SIZE}`);
